language: ga
responses:
  intents:
    HassMediaSearchAndPlay:
      default: |
        {% if slots.media: %}
        Ag seinm na meán
        {% else: %}
        Níor aimsíodh na meáin
        {% endif %}
