language: hi
responses:
  intents:
    HassLightSet:
      brightness: "{{ slots.name }} की चमक {{ slots.brightness }} कर दी"
      brightness_area: "{{ slots.area }} की चमक {{ slots.brightness }} कर दी"
      color: "{{ slots.name }} का रंग {{ slots.color }} कर दिया"
      color_area: "{{ slots.area }} का रंग {{ slots.color }} कर दिया"
      temperature: "{{ slots.name }} का रंग तापमान सेट कर दिया"
