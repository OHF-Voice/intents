language: sr-Latn
responses:
  intents:
    HassLightSet:
      brightness: "Jačina osvetljenosti za svetlo {{ slots.name }} je postavljena na {{ slots.brightness }}"
      brightness_area: "Jačina osvetljenosti za oblast {{ slots.area }} je postavljena na {{ slots.brightness }}"
      color: "Boja za svetlo {{ slots.name }} je postavljena na {{ slots.color }}"
      color_area: "Boja svetla za oblast {{ slots.area }} je postavljena na {{ slots.color }}"
