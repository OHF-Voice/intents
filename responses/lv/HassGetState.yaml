language: lv
responses:
  intents:
    HassGetState:
      default: "{{ state.state_with_unit }}" # used mostly for custom sentences

      one_yesno: |
        {% if query.matched %}
          Jā
        {% else %}
          Nē
        {% endif %}
