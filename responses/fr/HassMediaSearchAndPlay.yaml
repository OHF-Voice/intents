language: "fr"
responses:
  intents:
    HassMediaSearchAndPlay:
      default: |
        {% if slots.media: %}
        Lecture en cours
        {% else: %}
        Média introuvable
        {% endif %}
