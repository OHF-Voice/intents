language: zh-CN
responses:
  intents:
    HassMediaSearchAndPlay:
      default: |
        {% if slots.media is defined and slots.media: %}
        好的，正在播放
        {% else: %}
        抱歉，没找到要播放的内容
        {% endif %}
