language: zh-CN
responses:
  intents:
    HassGetWeather:
      default: |
        {% if slots.name %}
          {{ slots.name }}的天气是 {{ state.state }}
        {% else %}
          天气是 {{ state.state }}
        {% endif %}
