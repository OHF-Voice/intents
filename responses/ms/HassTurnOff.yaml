language: ms
responses:
  intents:
    HassTurnOff:
      default: "{{ slots.name }} telah dimatikan"
      lights_area: "Lampu telah dipadamkan"
      fans_area: "Kipas telah dimatikan"
      cover: "{{ slots.name }} telah ditutupkan"
      cover_device_class: "Menutup {{ slots.device_class }}"
