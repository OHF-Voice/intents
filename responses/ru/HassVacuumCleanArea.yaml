language: ru
responses:
  intents:
    HassVacuumCleanArea:
      predlog: "Запущена уборка в {{ slots.area }}"
      vin: "Убираю {{ slots.area }}"
      here: "Запущена уборка зоны: {{ slots.area }}"
