language: "en"
responses:
  intents:
    HassShoppingListCompleteItem:
      item_completed: |
        {% if slots.item %}
        Checked off {{ slots.item }}
        {% else %}
        Item not found
        {% endif %}
