language: "en"
responses:
  intents:
    HassShoppingListCompleteItem:
      item_completed: |
        {% if slots.item %}
        Checked off {{ slots.item }}
        {% else %}
        No matching item found on your shopping list.
        {% endif %}
