language: "en"
responses:
  intents:
    HassShoppingListCompleteItem:
      item_completed: |
        {% if slots.item in slots.completed_items %}
        Checked off {{ slots.item }}
        {% else %}
        Item not found
        {% endif %}
