language: sk
responses:
  intents:
    HassTurnOff:
      default: Vypínam {{ slots.name }}
      lights_area: Svetlá boli zhasnuté
      lights_floor: Svetlá boli zhasnuté
      light_all: Všetky svetlá boli zhasnuté
      cover_device_class: Zatváram {{ slots.device_class }}
      fans_area: Ventilátory v {{ slots.area }} boli vypnuté
      cover: Zatváram {% if slots.name | default(false) %}{{ slots.name }} {% else %} {{ slots.device_class }} {% endif %}
      lock:
        "Odomykám {% if slots.name == 'all' %} všetky zámky {% else %} {{ slots.name }} {% endif %}

        "
      valve: Zatváram {{ slots.name }}
