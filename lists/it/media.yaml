language: it
lists:
  volume_step:
    values:
      - in: "(su|alz(a|are)|aument(a|are)|increment(a|are))"
        out: "up"
      - in: "(giù|giu|abbass(a|are)|diminuisc(i|e)|decrement(a|are))"
        out: "down"
