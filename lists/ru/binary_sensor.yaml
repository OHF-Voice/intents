---
language: "ru"

lists:
  # open / closed -> on / off (door, garage_door, window)
  bs_open_states:
    values:
      - in: "открыт[о|а|ы]"
        out: "on"
      - in: "закрыт[о|а|ы]"
        out: "off"

  # occupancy
  bs_occupancy_states:
    values:
      - in: "(занят[а|о|ы]|[кто][-то] есть [кто][-то]|есть присутствие)"
        out: "on"
      - in: "(пуст[о]|никого [нет]|свободно|свободен|нет присутствия)"
        out: "off"
