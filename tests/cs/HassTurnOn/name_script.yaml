---
# HassTurnOn - name_script

language: "cs"
entities:
  - name: "úklid"
    domain: "script"

tests:
  - sentences:
      - "spusť skript úklid"
      - "aktivuj úklid"
      - "úklid"
    slots:
      name: "úklid"
    response: "Spuštěno"
