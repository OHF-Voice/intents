---
# HassTurnOn - name_script
# example: lance le script mode discret
# slots: {name}

language: fr
entities:
  - name: "Mode discret"
    domain: "script"

tests:
  - sentences:
      - "lance le mode discret"
      - "exécute le script mode discret"
      - "mode discret"
    slots:
      name: "Mode discret"
    response: "Script démarré"
