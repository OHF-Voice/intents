---
# HassStartTimer - command_seconds

language: ru
tests:
  - sentences:
      - включи торшер через 45 секунд
    slots:
      seconds: 45
      conversation_command:
        - включи торшер
    response: Команда будет выполнена через 45 секунд
  - sentences:
      - через 45 секунд включи торшер
    slots:
      seconds: 45
      conversation_command:
        - включи торшер
    response: Команда будет выполнена через 45 секунд
