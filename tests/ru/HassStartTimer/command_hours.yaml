---
# HassStartTimer - command_hours

language: ru
tests:
  - sentences:
      - включи торшер через 1 час
    slots:
      hours: 1
      conversation_command:
        - включи торшер
    response: Команда будет выполнена через 1 час
  - sentences:
      - через 1 час включи торшер
    slots:
      hours: 1
      conversation_command:
        - включи торшер
    response: Команда будет выполнена через 1 час
