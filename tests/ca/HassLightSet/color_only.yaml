---
# HassLightSet - color_only

language: ca
entities:
  - name: Llum
    domain: light
tests:
  - sentences:
      - posa els llums de color vermell
      - fes els llums vermells
    slots:
      color: red
    response: Color ajustat
