language: ca
tests:
  - sentences:
      - "està oberta la finestra del cobert?"
    intent:
      name: HassGetState
      slots:
        domain: "binary_sensor"
        device_class: "window"
        name: "Finestra del cobert"
        state: "on"
    response: "Si"
