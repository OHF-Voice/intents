---
# HassTurnOn - name_script

language: ca
entities:
  - name: Mode sigil
    domain: script
tests:
  - sentences:
      - executa l'script mode sigil
      - inicia el mode sigil
    slots:
      name: Mode sigil
    response: Iniciant
