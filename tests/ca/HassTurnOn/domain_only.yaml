---
# HassTurnOn - domain_only

language: ca
tests:
  - sentences:
      - encén els llums
      - engega els llums
      - obre els llums
    slots:
      domain: light
    response: Llums enceses a __context_area__
  - sentences:
      - encén els ventiladors
    slots:
      domain: fan
    response: Ventiladors encesos a __context_area__
