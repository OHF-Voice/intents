language: it
tests:
  - sentences:
      - "riprendi il timer chiamato pizza"
      - "riprendi il timer per pizza"
      - "riprendi il pizza timer"
    slots:
      name: "pizza"
    response: "Timer ripreso"
