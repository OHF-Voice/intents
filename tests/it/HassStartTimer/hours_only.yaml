language: it
tests:
  - sentences:
      - "avvia un timer per 1 ora"
      - "avvia un timer 1 ora"
      - "timer per 1 ora"
      - "timer 1 ora"
    slots:
      hours: 1
    response: "Timer avviato"
