language: it
tests:
  - sentences:
      - "avvia un timer per 5 minuti chiamato pizza"
      - "avvia un timer chiamato pizza per 5 minuti"
      - "timer per 5 minuti per pizza"
    slots:
      minutes: 5
      name: "pizza"
    response: "Timer avviato"
