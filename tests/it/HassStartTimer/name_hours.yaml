language: it
tests:
  - sentences:
      - "avvia un timer per 1 ora chiamato pizza"
      - "avvia un timer chiamato pizza per 1 ora"
      - "timer per 1 ora per pizza"
    slots:
      hours: 1
      name: "pizza"
    response: "Timer avviato"
