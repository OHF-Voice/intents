language: it
tests:
  - sentences:
      - "avvia un timer per 30 secondi"
      - "avvia un timer 30 secondi"
      - "timer per 30 secondi"
      - "timer 30 secondi"
    slots:
      seconds: 30
    response: "Timer avviato"
  - sentences:
      - "avvia un timer per mezzo minuto"
      - "timer per mezzo minuto"
    slots:
      seconds: 30
    response: "Timer avviato"
