language: it
tests:
  - sentences:
      - "avvia un timer per 30 secondi chiamato pizza"
      - "avvia un timer chiamato pizza per 30 secondi"
      - "timer per 30 secondi per pizza"
    slots:
      seconds: 30
      name: "pizza"
    response: "Timer avviato"
