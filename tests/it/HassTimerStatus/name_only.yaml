language: it
timers:
  - is_active: true
    name: "pizza"
    total_seconds_left: 300
    rounded_hours_left: 0
    rounded_minutes_left: 5
    rounded_seconds_left: 0
    start_minutes: 5
tests:
  - sentences:
      - "stato pizza timer"
      - "stato del mio timer chiamato pizza"
      - "tempo rimanente sul timer per pizza"
    slots:
      name: "pizza"
    response: "5 minuti rimanenti."
