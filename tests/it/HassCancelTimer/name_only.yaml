language: it
tests:
  - sentences:
      - "cancella il timer chiamato pizza"
      - "cancella il timer per pizza"
      - "cancella il pizza timer"
    slots:
      name: "pizza"
    response: "Timer cancellato"
