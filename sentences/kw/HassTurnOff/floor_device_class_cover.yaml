---
# HassTurnOff - floor_device_class_cover
# example: close the curtains on the first floor
# slots:
# - {floor}
# - {domain}
# - {device_class}

language: "kw"
data:
  - sentences:
      - "<close> [<all>] [<the>] {cover_classes:device_class} <in> [<the>] {floor} [leur]"
      - "<close> [<all>] [<the>] {floor} [leur] {cover_classes:device_class}"
    example: "dege an korglennow war an kynsa leur"
    inferred_domain: "cover"
    response: "cover_device_class"
