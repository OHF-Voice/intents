language: is
data:
  - sentences:
      - "<close> {cover_classes:device_class} (í|á) {area}[(inu|nu|ið|ð|inni|nni|num)]"
      - "<close> {area}[(s)]{cover_classes:device_class}"
    example: "Lokaðu gluggunum í eldhúsinu"
    inferred_domain: cover
    response: cover_device_class
