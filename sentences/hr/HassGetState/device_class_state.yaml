---
# HassGetState - device_class_state

language: "hr"
data:
  # ---- covers (by device class) ----
  - sentences:
      - "<je_li> [koji|koja|koje|neki] {cover_states:state} {cover_classes:device_class}"
      - "<je_li> [koji|koja|koje|neki] {cover_classes:device_class} {cover_states:state}"
    example: "je li koji prozor otvoren"
    inferred_domain: "cover"
    response: "any"
  - sentences:
      - "<je_li> (sva|svi|sve) {cover_states:state} {cover_classes:device_class}"
      - "<je_li> (sva|svi|sve) {cover_classes:device_class} {cover_states:state}"
    example: "jesu li svi prozori zatvoreni"
    inferred_domain: "cover"
    response: "all"
  - sentences:
      - "<koji_su> {cover_classes:device_class} [su] {cover_states:state}"
    example: "koji prozori su otvoreni"
    inferred_domain: "cover"
    response: "which"
  - sentences:
      - "<koliko> {cover_classes:device_class} {cover_states:state}"
      - "<koliko> {cover_states:state} {cover_classes:device_class}"
    example: "koliko je prozora otvoreno"
    inferred_domain: "cover"
    response: "how_many"
