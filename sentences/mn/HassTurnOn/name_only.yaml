---
# HassTurnOn - name_only
# example: унтлагын өрөөний гэрэл асаа
# slots: {name}

language: "mn"
data:
  # on-able domains (light, switch)
  - sentences:
      - "{name} асаа"
    example: "унтлагын өрөөний гэрэл асаа"
    name_domains:
      - "light"
      - "switch"
      - "climate"
    response: "default"
