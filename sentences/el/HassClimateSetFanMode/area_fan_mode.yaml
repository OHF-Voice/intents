language: el
data:
  - sentences:
      - "<set> [τη] λειτουργία [του] ανεμιστήρα <in> [<the>] {area} σε {fan_mode}"
      - "<set> [<the>] {area} [τη] λειτουργία ανεμιστήρα σε {fan_mode}"
    example: όρισε τη λειτουργία ανεμιστήρα στο υπνοδωμάτιο σε αυτόματο
    response: default
