language: ca
intents:
  HassGetState:
    data:
      - sentences:
          - "<esta> {bs_window_states:state} [<pronom> ]{name}"
        response: one_yesno
        requires_context:
          domain: binary_sensor
          device_class: window
        slots:
          domain: binary_sensor
          device_class: window
