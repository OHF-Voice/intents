language: ca
intents:
  HassGetState:
    data:
      - sentences:
          - "<esta> {bs_window_states:state} [<pronom> ]<name>"
          - "<esta> {bs_window_states:state} [<pronom> ]<name>[[ <preposicio_base>][ <pronom>] {area}]"
        response: one_yesno
        requires_context:
          domain: binary_sensor
          device_class: window
        slots:
          domain: binary_sensor
          device_class: window
      - sentences:
          - "<esta> {bs_door_states:state} [<pronom> ]<name>"
          - "<esta> {bs_door_states:state} [<pronom> ]<name>[[ <preposicio_base>][ <pronom>] {area}]"
        response: one_yesno
        requires_context:
          domain: binary_sensor
          device_class: door
        slots:
          domain: binary_sensor
          device_class: door
