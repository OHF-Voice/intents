---
# HassGetState - device_class_state
# slots: {domain}, {device_class}, {state}

language: "ca"
data:
  - sentences:
      - "<are_any> {cover_classes:device_class} [<is>] {cover_states:state}"
    inferred_domain: "cover"
    example: "consulta estat"
    response: "any"
  - sentences:
      - "<are_all> {cover_classes:device_class} [<is>] {cover_states:state}"
    inferred_domain: "cover"
    example: "consulta estat"
    response: "all"
  - sentences:
      - "<which> {cover_classes:device_class} <is> {cover_states:state}"
    inferred_domain: "cover"
    example: "consulta estat"
    response: "which"
  - sentences:
      - "<how_many> {cover_classes:device_class} <is> {cover_states:state}"
    inferred_domain: "cover"
    example: "consulta estat"
    response: "how_many"
