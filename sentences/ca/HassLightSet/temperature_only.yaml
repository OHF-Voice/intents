---
# HassLightSet - temperature_only
# example: posa la temperatura de color a 2700 kelvin
# slots: {temperature}; context_area: true

language: "ca"
data:
  - sentences:
      - "<configura> [la] temperatura [de color] (a|al) {color_temperature:temperature}[[ ](k|kelvin)]"
      - "<configura> [la] temperatura [de color] (a|al) {color_temperature_names:temperature}"
    example: "posa la temperatura de color a 2700 kelvin"
    inferred_domain: "light"
    response: "temperature"
