---
# HassLightSet - name_color
# example: sæt soveværelseslampens farve til rød
# slots:
# - {name}
# - {color}

language: "da"
data:
  - sentences:
      - "<sæt_numerisk_værdi> <navn> ([<farve>];til) <farve_input>"
      - "<sæt_numerisk_værdi> <farve> på <navn> til <farve_input>"
      - "<sæt_numerisk_værdi> <farve_input> på <navn>"
    example: "sæt soveværelseslampens farve til rød"
    name_domains:
      - "light"
    response: "color"
