---
# HassLightSet - color_only
# example: aseta valot punaiseksi

language: "fi"
data:
  - sentences:
      - "[<laita>] [kaikki] (<valo>|<valot>) [(<väri>|<väriksi>|<väriin>|<värille>|värit|väreiksi|väreihin|väreille|väriltään)] {color}"
      - "[<laita>] (<väri>|<väriksi>|<väriin>|<värille>|värit|väreiksi|väreihin|väreille|väriltään) [kaikki] (<valo>|<valot>) {color}"
    example: "aseta valot punaiseksi"
    inferred_domain: "light"
    response: "color"
