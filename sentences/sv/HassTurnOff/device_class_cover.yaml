---
# HassTurnOff - device_class_cover
# example: stäng gardinerna
# slots:
# - {domain}
# - {device_class}
# context_area: true

language: "sv"
data:
  - sentences:
      - "<stäng_gardiner> {cover_classes:device_class}"
    example: "stäng gardinerna"
    inferred_domain: "cover"
    response: "cover_device_class"
