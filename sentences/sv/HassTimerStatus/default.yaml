---
# HassTimerStatus - default
# example: how much time is left on my timer

language: "sv"
data:
  - sentences:
      - "(<timers>|<timer>) status"
      - "status på [min[a] ](<timers>|<timer>)"
      - "[<time_left> ]tid [är det ]kvar på [min[a] |mitt ](<timers>|<timer>)"
    example: "hur mycket tid är kvar på min timer"
    response: "default"
