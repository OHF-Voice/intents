language: it
data:
  - sentences:
      - "{timer_command:conversation_command} (tra|fra|dopo) {timer_minutes:minutes} minut(o|i)"
      - "(tra|fra|dopo) {timer_minutes:minutes} minut(o|i) {timer_command:conversation_command}"
    example: "spegni le luci tra 5 minuti"
    response: "command"
