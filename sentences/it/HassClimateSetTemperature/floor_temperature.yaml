---
# HassClimateSetTemperature - floor_temperature
# example: imposta la temperatura del primo piano a 20 gradi
# slots:
# - {floor}
# - {temperature}

language: "it"
data:
  - sentences:
      - "<numeric_value_set> [<the>] <temp> (<of>|<on>) {floor} <to> {temperature}[([ ]°)| grad(o|i)]"
      - "<numeric_value_set> [<the>] {floor} <temp> <to> {temperature}[([ ]°)| grad(o|i)]"
    example: "imposta la temperatura del primo piano a 20 gradi"
    response: "default"
