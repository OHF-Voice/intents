---
# HassTurnOff - device_class_cover
# example: close the windows in here
# slots:
# - {domain}
# - {device_class}
# context_area: true

language: "it"
data:
  - sentences:
      - "<close> [<all>] [<the>] {cover_classes:device_class} [<in_here>]"
    example: "chiudi le tende qui"
    inferred_domain: "cover"
    response: "cover_device_class"
