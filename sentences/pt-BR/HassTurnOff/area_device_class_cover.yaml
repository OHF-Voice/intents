---
# HassTurnOff - area_device_class_cover
# example: fecha as cortinas da sala de estar
# slots:
# - {area}
# - {domain}
# - {device_class}

language: pt-BR
data:
  - sentences:
      - "<fechar> [(o|a|os|as)] {cover_classes:device_class} (em|no|na|do|da) {area}"
      - "<fechar> [(o|a|os|as)] {area} {cover_classes:device_class}"
    example: "fecha as cortinas da sala de estar"
    inferred_domain: "cover"
    response: "cover_device_class"
