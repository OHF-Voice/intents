---
# HassTurnOn - device_class_cover
# example: abre as cortinas
# slots:
# - {domain}
# - {device_class}
# context_area: true

language: pt-BR
data:
  - sentences:
      - "<abrir> [(o|a|os|as)] {cover_classes:device_class}"
      - "<abrir> [(o|a|os|as)] {cover_classes:device_class} (aqui|neste cômodo|nesta sala)"
    example: "abre as cortinas"
    inferred_domain: "cover"
    response: "cover_device_class"
