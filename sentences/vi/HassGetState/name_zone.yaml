---
# HassGetState - name_zone
# example: jane có ở nhà không
# slots:
# - {name}
# - {state}

language: "vi"
data:
  - sentences:
      - "{name} <state_q> [hiện tại] [<in>] {zone:state} không"
    example: "jane có ở nhà không"
    name_domains:
      - "person"
    response: "one_yesno"
