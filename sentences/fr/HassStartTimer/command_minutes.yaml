---
# HassStartTimer - command_minutes
language: fr
data:
  - sentences:
      - "{timer_command:conversation_command} dans {timer_minutes:minutes} <minute_unit>"
      - "Dans {timer_minutes:minutes} <minute_unit> {timer_command:conversation_command}"
    example: "éteins la lumière dans 5 minutes"
    response: "command"
