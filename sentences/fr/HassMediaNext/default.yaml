---
# HassMediaNext - default
# example: suivant
# context_area: true

language: fr
data:
  - sentences:
      - "suivant[e]"
      - "<media> suivant[e]"
      - "<mets> (<le>|au |à la )<media> suivant[e]"
    example: "suivant"
    response: "default"
