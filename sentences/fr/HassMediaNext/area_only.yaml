---
# HassMediaNext - area_only
# example: suivant dans le salon
# slots: {area}

language: fr
data:
  - sentences:
      - "suivant[e] <dans> [<le>]{area}"
      - "<media> suivant[e] <dans> [<le>]{area}"
      - "<mets> (<le>|au |à la )<media> suivant[e] <dans> [<le>]{area}"
    example: "suivant dans le salon"
    response: "default"
