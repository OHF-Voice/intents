---
# HassGetState - device_class_state_floor
# slots:
# - {floor}
# - {domain}
# - {device_class}
# - {state}

language: fr
data:
  # ---- covers (by device class) ----
  - sentences:
      - "<gs_any> {cover_classes:device_class} [<estil>] {cover_states:state} <dans> [<le>]{floor}"
    example: "y a-t-il des fenêtres ouvertes au premier étage"
    inferred_domain: "cover"
    response: "any"
  - sentences:
      - "<gs_all> {cover_classes:device_class} [<estil>] {cover_states:state} <dans> [<le>]{floor}"
    example: "toutes les fenêtres sont-elles fermées au premier étage"
    inferred_domain: "cover"
    response: "all"
  - sentences:
      - "<gs_which> {cover_classes:device_class} [<estil>] {cover_states:state} <dans> [<le>]{floor}"
    example: "quelles fenêtres sont ouvertes au premier étage"
    inferred_domain: "cover"
    response: "which"
  - sentences:
      - "<gs_howmany> {cover_classes:device_class} [<estil>] {cover_states:state} <dans> [<le>]{floor}"
    example: "combien de fenêtres sont ouvertes au premier étage"
    inferred_domain: "cover"
    response: "how_many"
