---
language: fr
intents:
  HassSetVolumeRelative:
    data:
      # Current area
      - sentences:
          - "monte [le] volume"
          - "augmente [le] volume"
        slots:
          volume_step: "up"
        requires_context:
          area:
            slot: true
      - sentences:
          - "monte [le] volume [de] <volume_step> <pourcent>"
          - "augmente [le] volume [de] <volume_step> <pourcent>"
        expansion_rules:
          volume_step: "{volume_step_up:volume_step}[%| percent]"
        requires_context:
          area:
            slot: true

      - sentences:
          - "baisse [le] volume"
          - "diminue [le] volume"
        slots:
          volume_step: "down"
        requires_context:
          area:
            slot: true
      - sentences:
          - "baisse [le] volume [de] <volume_step> <pourcent>"
          - "diminue [le] volume [de] <volume_step> <pourcent>"
        expansion_rules:
          volume_step: "{volume_step_down:volume_step}[%| percent]"
        requires_context:
          area:
            slot: true

      # Media player name
      - sentences:
          - "monte [le] volume [<de>] [<le>] <name>"
          - "augmente [le] volume [<de>] [<le>] <name>"
        slots:
          volume_step: "up"
        requires_context:
          domain: "media_player"
      - sentences:
          - "monte [le] volume ([<de>] [<le>] <name>;[de] <volume_step> <pourcent>)"
          - "augmente [le] volume ([<de>] [<le>] <name>;[de] <volume_step> <pourcent>)"
        expansion_rules:
          volume_step: "{volume_step_up:volume_step}[%| percent]"
        slots:
          volume_step: "up"
        requires_context:
          domain: "media_player"

      - sentences:
          - "diminue [le] volume [<de>] [<le>] <name>"
          - "baisse [le] volume [<de>] [<le>] <name>"
        slots:
          volume_step: "down"
        requires_context:
          domain: "media_player"
      - sentences:
          - "baisse [le] volume ([<de>] [<le>] <name>;[de] <volume_step> <pourcent>)"
          - "diminue [le] volume ([<de>] [<le>] <name>;[de] <volume_step> <pourcent>)"
        expansion_rules:
          volume_step: "{volume_step_down:volume_step}[%| percent]"
        slots:
          volume_step: "down"
        requires_context:
          domain: "media_player"

      # Area name
      - sentences:
          - "monte [le] volume <dans> [<le>]{area}"
          - "augmente [le] volume <dans> [<le>]{area}"
        slots:
          volume_step: "up"
      - sentences:
          - "monte [le] volume (<dans> [<le>]{area};[de] <volume_step> <pourcent>)"
          - "augmente [le] volume (<dans> [<le>]{area};[de] <volume_step> <pourcent>)"
        expansion_rules:
          volume_step: "{volume_step_up:volume_step}[%| percent]"

      - sentences:
          - "baisse [le] volume <dans> [<le>]{area}"
          - "diminue [le] volume <dans> [<le>]{area}"
        slots:
          volume_step: "down"
      - sentences:
          - "baisse [le] volume (<dans> [<le>]{area};[de] <volume_step> <pourcent>)"
          - "diminue [le] volume (<dans> [<le>]{area};[de] <volume_step> <pourcent>)"
        expansion_rules:
          volume_step: "{volume_step_down:volume_step}[%| percent]"
