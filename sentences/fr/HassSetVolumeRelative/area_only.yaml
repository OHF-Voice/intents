---
# HassSetVolumeRelative - area_only
# example: monte le volume dans la cuisine
# slots:
# - {volume_step}
# - {area}

language: fr
data:
  # up
  - sentences:
      - "(<augmente>|<monte>) [<le>]<volume> <dans> [<le>]{area}"
    slots:
      volume_step: "up"
    example: "Monte le volume dans la cuisine"
    response: "default"

  # down
  - sentences:
      - "(<diminue>|<baisse>) [<le>]<volume> <dans> [<le>]{area}"
    slots:
      volume_step: "down"
    example: "Baisse le volume dans le salon"
    response: "default"
