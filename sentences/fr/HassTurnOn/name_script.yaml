---
# HassTurnOn - name_script
# example: lance le script mode discret
# slots: {name}

language: fr
data:
  - sentences:
      - "<active> [le script] {name}"
      - "[<active>] [<le>]{name}"
    example: "lance le script mode discret"
    name_domains:
      - "script"
    response: "script"
