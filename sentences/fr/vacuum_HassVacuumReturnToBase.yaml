language: fr
intents:
  HassVacuumReturnToBase:
    data:
      - sentences:
          # Renvoyer Alfred à sa base
          - "<renvoie> [<le>]{name} [(à|sur) (sa|la) base]"
          # Arrête Alfred
          - "<eteins> [<le>]{name}"
          # Range Alfred
          - "<range> [<le>]{name}"
        requires_context:
          domain: vacuum
