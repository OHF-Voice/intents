language: sk
data:
  - sentences:
      - "[<timer_set> ]<timer_type> na <dur_m> {timer_name:name}"
      - "[<timer_set> ]<timer_type> {timer_name:name} na <dur_m>"
      - "[<timer_set> ]<dur_m> <timer_type> {timer_name:name}"
    response: default
