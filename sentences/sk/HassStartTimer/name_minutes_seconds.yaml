language: sk
data:
  - sentences:
      - "[<timer_set> ]<timer_type> na <dur_ms> {timer_name:name}"
      - "[<timer_set> ]<timer_type> {timer_name:name} na <dur_ms>"
      - "[<timer_set> ]<dur_ms> <timer_type> {timer_name:name}"
    response: default
