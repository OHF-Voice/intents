language: sk
data:
  - sentences:
      - "[<timer_set> ]<timer_type> na <dur_hms> {timer_name:name}"
      - "[<timer_set> ]<timer_type> {timer_name:name} na <dur_hms>"
      - "[<timer_set> ]<dur_hms> <timer_type> {timer_name:name}"
    response: default
