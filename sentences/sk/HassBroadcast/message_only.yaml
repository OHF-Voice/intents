language: sk
data:
  - sentences:
      - "<broadcast> [<everywhere>] [<that>] {message}"
      - "<broadcast> [<everywhere>] [nasledujúcu] správu {message}"
    example: "oznám, že večera je hotová"
    response: default
