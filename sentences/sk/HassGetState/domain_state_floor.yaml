language: sk
data:
  - sentences:
      - "<are_any> <light> {on_off_states:state} <floor>"
    inferred_domain: "light"
    response: any
  - sentences:
      - "<are_all> <light> {on_off_states:state} <floor>"
    inferred_domain: "light"
    response: all
  - sentences:
      - "<which> <light> <is> {on_off_states:state} <floor>"
    inferred_domain: "light"
    response: which
  - sentences:
      - "<how_many> <light> <is> {on_off_states:state} <floor>"
    inferred_domain: "light"
    response: how_many
  - sentences:
      - "<are_any> (ventilátor[y|ov]|vetrák[y|ov]) {on_off_states:state} <floor>"
    inferred_domain: "fan"
    response: any
  - sentences:
      - "<are_all> (ventilátor[y|ov]|vetrák[y|ov]) {on_off_states:state} <floor>"
    inferred_domain: "fan"
    response: all
  - sentences:
      - "<which> (ventilátor[y|ov]|vetrák[y|ov]) <is> {on_off_states:state} <floor>"
    inferred_domain: "fan"
    response: which
  - sentences:
      - "<how_many> (ventilátor[y|ov]|vetrák[y|ov]) <is> {on_off_states:state} <floor>"
    inferred_domain: "fan"
    response: how_many
  - sentences:
      - "<are_any> (vypínač[e|ov]|spínač[e|ov]) {on_off_states:state} <floor>"
    inferred_domain: "switch"
    response: any
  - sentences:
      - "<are_all> (vypínač[e|ov]|spínač[e|ov]) {on_off_states:state} <floor>"
    inferred_domain: "switch"
    response: all
  - sentences:
      - "<which> (vypínač[e|ov]|spínač[e|ov]) <is> {on_off_states:state} <floor>"
    inferred_domain: "switch"
    response: which
  - sentences:
      - "<how_many> (vypínač[e|ov]|spínač[e|ov]) <is> {on_off_states:state} <floor>"
    inferred_domain: "switch"
    response: how_many
  - sentences:
      - "<are_any> <lockable> {lock_states:state} <floor>"
    inferred_domain: "lock"
    response: any
  - sentences:
      - "<are_all> <lockable> {lock_states:state} <floor>"
    inferred_domain: "lock"
    response: all
  - sentences:
      - "<which> <lockable> <is> {lock_states:state} <floor>"
    inferred_domain: "lock"
    response: which
  - sentences:
      - "<how_many> <lockable> <is> {lock_states:state} <floor>"
    inferred_domain: "lock"
    response: how_many
