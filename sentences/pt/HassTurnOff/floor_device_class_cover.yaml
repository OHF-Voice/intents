---
# HassTurnOff - floor_device_class_cover
# example: fecha as cortinas do primeiro andar
# slots:
# - {floor}
# - {domain}
# - {device_class}

language: "pt"
data:
  - sentences:
      - "<fechar> [tod(as|os)] [(o|a|os|as)] {cover_classes:device_class} (em|no|na|do|da) [andar|piso] {floor} [andar|piso]"
      - "<fechar> [tod(as|os)] [(o|a|os|as)] [andar|piso] {floor} {cover_classes:device_class}"
    example: "fecha as cortinas do primeiro andar"
    inferred_domain: "cover"
    response: "cover_device_class"
