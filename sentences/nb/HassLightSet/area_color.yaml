language: "nb"
data:
  - sentences:
      - "<set> farge[n] <in> [den ]{area} til {color}"
      - "<set> [den ]{area} [farge] til {color}"
      - "<set> [(<all>|den)] <light> <in> [den ]{area} til {color}"
    example: "sett fargen i soverommet til rød"
    inferred_domain: "light"
    response: "color"
