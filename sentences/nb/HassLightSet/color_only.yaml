language: "nb"
data:
  - sentences:
      - "<set> [(<all>|den)] <light> [<here>] [til] {color}"
      - "<set> farge[n] på [(<all>|den)] <light> [<here>] til {color}"
    example: "gjør lyset rødt"
    inferred_domain: "light"
    response: "color"
