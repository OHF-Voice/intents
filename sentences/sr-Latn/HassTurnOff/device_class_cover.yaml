---
# HassTurnOff - device_class_cover
# example: zatvori prozore
# slots:
# - {domain}
# - {device_class}
# context_area: true

language: "sr-Latn"
data:
  - sentences:
      - "<close> {cover_classes:device_class}"
      - "<close> {cover_classes:device_class} [ovde|u ovoj prostoriji]"
    example: "zatvori prozore"
    inferred_domain: "cover"
    response: "cover_device_class"
