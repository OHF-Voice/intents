---
# HassTimerStatus - name_only
# example: status minutnika pizza

language: "pl"
data:
  - sentences:
      - "<timer_state> (minutnik[a]|timer[a]|stoper[a]|licznik[a]) [o nazwie |nazwany ]{timer_name:name}"
      - "ile czasu [po]zostało na (minutnik[u]|timer[ze]|stoper[ze]|licznik[u]) [o nazwie |nazwany ]{timer_name:name}"
    example: "status minutnika pizza"
    response: "default"
