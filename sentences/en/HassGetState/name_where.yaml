---
# HassGetState - name_where
# example: where is jane
# slots: {name}

language: "en"
data:
  - sentences:
      - "<where_is> [<the>] {name}"
      - "where can I find [<the>] {name}"
    example: "where is jane"
    name_domains:
      - "person"
    response: "where"
