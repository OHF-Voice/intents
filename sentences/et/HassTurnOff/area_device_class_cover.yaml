---
# HassTurnOff - area_device_class_cover
# example: sulge elutoa kardinad
# slots:
# - {area}
# - {domain}
# - {device_class}

language: "et"
data:
  - sentences:
      - "<close> [<the>] {area} {cover_classes:device_class}"
    example: "sulge köögi kardinad"
    inferred_domain: "cover"
    response: "cover_device_class"
