---
# HassGetState - domain_state
# example: чи всі світло вимкнено
# slots:
# - {domain}
# - {state}

language: "uk"
data:
  # ---- lights ----
  - sentences:
      - "<are_any> (світло|світильник[и]|ламп[и]) {on_off_states:state}"
    example: "чи є світло увімкнено"
    inferred_domain: "light"
    response: "any"
  - sentences:
      - "<are_all> (світло|світильник[и]|ламп[и]) {on_off_states:state}"
    example: "чи всі світло вимкнено"
    inferred_domain: "light"
    response: "all"
  - sentences:
      - "<which> (світло|світильник[и]|ламп[и]) {on_off_states:state}"
    example: "які світло увімкнено"
    inferred_domain: "light"
    response: "which"
  - sentences:
      - "<how_many> (світло|світильник[и]|ламп[и]) {on_off_states:state}"
    example: "скільки світло увімкнено"
    inferred_domain: "light"
    response: "how_many"

  # ---- fans ----
  - sentences:
      - "<are_any> (вентилятор|вентилятори|вентиляторів) {on_off_states:state}"
    example: "чи є вентилятори увімкнено"
    inferred_domain: "fan"
    response: "any"
  - sentences:
      - "<are_all> (вентилятор|вентилятори|вентиляторів) {on_off_states:state}"
    example: "чи всі вентилятори вимкнено"
    inferred_domain: "fan"
    response: "all"
  - sentences:
      - "<which> (вентилятор|вентилятори|вентиляторів) {on_off_states:state}"
    example: "які вентилятори увімкнено"
    inferred_domain: "fan"
    response: "which"
  - sentences:
      - "<how_many> (вентилятор|вентилятори|вентиляторів) {on_off_states:state}"
    example: "скільки вентиляторів увімкнено"
    inferred_domain: "fan"
    response: "how_many"

  # ---- switches ----
  - sentences:
      - "<are_any> (вмикач|вмикачі|вмикачів|вимикач|вимикачі|вимикачів) {on_off_states:state}"
    example: "чи є вимикачі увімкнено"
    inferred_domain: "switch"
    response: "any"
  - sentences:
      - "<are_all> (вмикач|вмикачі|вмикачів|вимикач|вимикачі|вимикачів) {on_off_states:state}"
    example: "чи всі вимикачі вимкнено"
    inferred_domain: "switch"
    response: "all"
  - sentences:
      - "<which> (вмикач|вмикачі|вмикачів|вимикач|вимикачі|вимикачів) {on_off_states:state}"
    example: "які вимикачі увімкнено"
    inferred_domain: "switch"
    response: "which"
  - sentences:
      - "<how_many> (вмикач|вмикачі|вмикачів|вимикач|вимикачі|вимикачів) {on_off_states:state}"
    example: "скільки вимикачів увімкнено"
    inferred_domain: "switch"
    response: "how_many"

  # ---- locks ----
  - sentences:
      - "<are_any> <lockable> {lock_states:state}"
    example: "чи є двері відчинено"
    inferred_domain: "lock"
    response: "any"
  - sentences:
      - "<are_all> <lockable> {lock_states:state}"
    example: "чи всі двері замкнено"
    inferred_domain: "lock"
    response: "all"
  - sentences:
      - "<which> <lockable> {lock_states:state}"
    example: "які двері відчинено"
    inferred_domain: "lock"
    response: "which"
  - sentences:
      - "<how_many> <lockable> {lock_states:state}"
    example: "скільки дверей замкнено"
    inferred_domain: "lock"
    response: "how_many"
