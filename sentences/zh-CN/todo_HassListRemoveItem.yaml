language: zh-CN
intents:
  HassListRemoveItem:
    data:
      - sentences:
          - "把{item}从<name>删掉"
          - "删除<name>里的{item}"
          - "移除<name>里的{item}"
          - "{item}从<name>移除"
        response: item_deleted
        requires_context:
          domain: todo
        slots:
          item:
            wildcard: true
