language: zh-CN
intents:
  HassMediaSearchAndPlay:
    data:
      - sentences:
          - "在{name} 播放{search_query}"
          - "用{name} 播放{search_query}"
          - "在{name} 上播放{search_query}"
        slots:
          name:
            wildcard: true
          search_query:
            wildcard: true
