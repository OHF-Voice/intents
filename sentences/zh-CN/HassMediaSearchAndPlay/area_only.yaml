language: zh-CN
intents:
  HassMediaSearchAndPlay:
    data:
      - sentences:
          - "在{area} 播放{search_query}"
          - "在{area} 放{search_query}"
          - "{area} 播放{search_query}"
        slots:
          area:
            wildcard: true
          search_query:
            wildcard: true
