language: zh-CN
intents:
  HassListAddItem:
    data:
      - sentences:
          - "把{item}加到<name>"
          - "<name>添加{item}"
          - "在<name>里加{item}"
          - "往<name>加{item}"
        response: item_added
        requires_context:
          domain: todo
        slots:
          item:
            wildcard: true
