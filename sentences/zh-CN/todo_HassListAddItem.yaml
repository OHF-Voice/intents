language: zh-CN
intents:
  HassListAddItem:
    data:
      - sentences:
          - "待办清单添加{item}"
          - "待办清单加上{item}"
          - "添加{item}到待办清单"
          - "加上{item}到待办清单"
          - "[在] 待办清单 [里] 添加{item}"
          - "[在] 待办清单 [里] 加上{item}"
        slots:
          item:
            wildcard: true
        requires_context:
          list_type: todo
