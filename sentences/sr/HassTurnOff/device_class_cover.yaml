---
# HassTurnOff - device_class_cover
# example: затвори прозоре
# slots:
# - {domain}
# - {device_class}
# context_area: true

language: "sr"
data:
  - sentences:
      - "<close> {cover_classes:device_class}"
      - "<close> {cover_classes:device_class} [овде|у овој просторији]"
    example: "затвори прозоре"
    inferred_domain: "cover"
    response: "cover_device_class"
