---
# HassTurnOff - area_domain
# example: искључи светла у дневној соби
# slots:
# - {area}
# - {domain}

language: "sr"
data:
  # lights
  - sentences:
      - "<деактивирај> [сва] <светла> (у|на) [просторији] {area}"
      - "<деактивирај> (у|на) [просторији] {area} [сва] <светла>"
    example: "искључи светла у дневној соби"
    inferred_domain: "light"
    response: "lights_area"

  # fans
  - sentences:
      - "<деактивирај> [све] вентилатор[е] (у|на) [просторији] {area}"
      - "<деактивирај> (у|на) [просторији] {area} [све] вентилатор[е]"
    example: "искључи вентилаторе у дневној соби"
    inferred_domain: "fan"
    response: "fans_area"
