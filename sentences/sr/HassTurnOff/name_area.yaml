---
# HassTurnOff - name_area
# example: искључи плафонско светло у дневној соби
# slots:
# - {name}
# - {area}

language: "sr"
data:
  # off-able domains
  - sentences:
      - "<deactivate> {name} (у|на) [просторији] {area}"
      - "<deactivate> (у|на) [просторији] {area} {name}"
    example: "искључи плафонско светло у дневној соби"
    name_domains: "default"
    response: "default"

  # lights named with a trailing "light(s)" word
  - sentences:
      - "<deactivate> {name} <light> (у|на) [просторији] {area}"
      - "<deactivate> (у|на) [просторији] {area} {name} <light>"
    example: "искључи кутак за игру светла у дневној соби"
    name_domains:
      - "light"
    response: "default"

  # covers
  - sentences:
      - "<close> {name} (у|на) [просторији] {area}"
    example: "затвори леву завесу у дневној соби"
    name_domains:
      - "cover"
    response: "cover"

  # valves
  - sentences:
      - "<close> {name} (у|на) [просторији] {area}"
    example: "затвори главни вентил у кухињи"
    name_domains:
      - "valve"
    response: "valve"

  # locks
  - sentences:
      - "откључај {name} (у|на) [просторији] {area}"
    example: "откључај улазна врата у дневној соби"
    name_domains:
      - "lock"
    response: "lock"
