---
# HassTurnOn - area_domain
# example: укључи светла у дневној соби
# slots:
# - {area}
# - {domain}

language: "sr"
data:
  # lights
  - sentences:
      - "<activate> [сва] <light> (у|на) [просторији] {area}"
      - "<activate> (у|на) [просторији] {area} [сва] <light>"
    example: "укључи светла у дневној соби"
    inferred_domain: "light"
    response: "lights_area"

  # fans
  - sentences:
      - "<activate> [све] вентилатор[е] (у|на) [просторији] {area}"
      - "<activate> (у|на) [просторији] {area} [све] вентилатор[е]"
    example: "укључи вентилаторе у дневној соби"
    inferred_domain: "fan"
    response: "fans_area"
