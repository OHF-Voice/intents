language: bg
data:
  - sentences:
      - "<close> [<all>] {cover_classes:device_class} на {floor}"
      - "<close> [<all>] на {floor} {cover_classes:device_class}"
    example: "затвори щорите на първия етаж"
    inferred_domain: "cover"
    response: "cover_device_class"
