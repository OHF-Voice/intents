language: bg
data:
  - sentences:
      - "<close> [<all>] {cover_classes:device_class} [в|на|във] {area}"
      - "<close> [<all>] [в|на|във] {area} {cover_classes:device_class}"
    example: "затвори щорите в спалнята"
    inferred_domain: "cover"
    response: "cover_device_class"
