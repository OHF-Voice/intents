language: bg
data:
  - sentences:
      - "<open> [<all>] {cover_classes:device_class} на {floor}"
      - "<open> [<all>] на {floor} {cover_classes:device_class}"
    example: "отвори щорите на първия етаж"
    inferred_domain: "cover"
    response: "cover_device_class"
