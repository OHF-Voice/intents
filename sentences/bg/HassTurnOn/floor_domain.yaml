language: bg
data:
  # lights
  - sentences:
      - "<turn_on> [<all>] <lights> на {floor}"
      - "<turn_on> [<all>] на {floor} <lights>"
    example: "включи лампите на първия етаж"
    inferred_domain: "light"
    response: "lights_floor"
