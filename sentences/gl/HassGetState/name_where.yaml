---
# HassGetState - name_where
# slots: {name}

language: gl
data:
  - sentences:
      - "<where_is> [o |a |os |as ]{name}"
      - "onde podo atopar [a |o ]{name}"
    example: onde está Ada
    name_domains:
      - person
    response: where
