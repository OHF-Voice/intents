---
# HassTurnOff - device_class_cover
# context_area: true
# slots: {domain}, {device_class}

language: gl
data:
  - sentences:
      - "<pecha> [o |a |os |as ]{cover_classes:device_class}"
      - "<pecha> [o |a |os |as ]{cover_classes:device_class} <de_aqui>"
    example: pecha as cortinas de aquí
    inferred_domain: cover
    response: cover_device_class
