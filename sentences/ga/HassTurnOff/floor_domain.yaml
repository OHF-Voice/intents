language: ga
data:
  - sentences:
      - "<turn_off> [(<all>|<the>)] <light> <in> [<the>] {floor}"
      - "cuir [(<all>|<the>)] <light> as <in> [<the>] {floor}"
      - "<turn_off> [(<all>|<the>)] <light> [<the>] {floor}"
    example: "múch na soilse ar an gcéad urlár"
    inferred_domain: "light"
    response: "lights_floor"
