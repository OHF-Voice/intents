---
# HassTurnOn - area_device_class_cover
# example: atver aizkarus viesistabā
# slots:
# - {area}
# - {domain}
# - {device_class}

language: "lv"
data:
  - sentences:
      - "<open> [vis(as|us)] {cover_classes:device_class} {area}"
      - "<open> {area} [vis(as|us)] {cover_classes:device_class}"
    example: "atver aizkarus viesistabā"
    inferred_domain: "cover"
    response: "cover_device_class"
