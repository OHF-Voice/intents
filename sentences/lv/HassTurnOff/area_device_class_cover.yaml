---
# HassTurnOff - area_device_class_cover
# example: aizver aizkarus viesistabā
# slots:
# - {area}
# - {domain}
# - {device_class}

language: "lv"
data:
  - sentences:
      - "<close> [vis(as|us)] {cover_classes:device_class} {area}"
      - "<close> {area} [vis(as|us)] {cover_classes:device_class}"
    example: "aizver aizkarus viesistabā"
    inferred_domain: "cover"
    response: "cover_device_class"
