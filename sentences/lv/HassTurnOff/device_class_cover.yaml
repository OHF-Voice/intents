---
# HassTurnOff - device_class_cover
# example: aizver aizkarus
# slots:
# - {domain}
# - {device_class}
# context_area: true

language: "lv"
data:
  - sentences:
      - "<close> [vis(as|us)] {cover_classes:device_class} [šeit]"
    example: "aizver aizkarus"
    inferred_domain: "cover"
    response: "cover_device_class"
