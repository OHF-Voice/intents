---
# HassTurnOn - device_class_cover
# example: open the curtains in here
# slots: {domain}, {device_class}
# context_area: true

language: "ro"
data:
  - sentences:
      - "<deschide> {cover_classes:device_class}[ <de_aici>]"
    example: "deschide perdelele"
    inferred_domain: "cover"
    response: "cover_device_class"
