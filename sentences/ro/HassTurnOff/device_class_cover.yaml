---
# HassTurnOff - device_class_cover
# example: close the curtains in here
# slots: {domain}, {device_class}
# context_area: true

language: "ro"
data:
  - sentences:
      - "<inchide> {cover_classes:device_class}[ <de_aici>]"
    example: "închide perdelele"
    inferred_domain: "cover"
    response: "cover_device_class"
