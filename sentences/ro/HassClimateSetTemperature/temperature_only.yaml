---
# HassClimateSetTemperature - temperature_only
# example: set temperature to 20 degrees
# slots: {temperature}
# context_area: true

language: "ro"
data:
  - sentences:
      - "<seteaza> (temperatur(ă|a) [aerului];[<pana_la>] <temperature>)"
      - "<modifica_temperatura> [<pana_la>] <temperature>"
      - "<modifica_temperatura> ((temperatur(ă|a) [aerului]|aerul);[<pana_la>] <temperature>)"
    example: "setează temperatura la 20 de grade"
    response: "default"
