---
# HassGetState - domain_state_area

# slots: area, domain, state
language: "ro"
data:
  - sentences:
      - "<are_any> <luminile> {on_off_states:state} [<din>] {area}"
    example: "X X"
    inferred_domain: "light"
    response: "any"
  - sentences:
      - "<are_all> <luminile> {on_off_states:state} [<din>] {area}"
    example: "X X"
    inferred_domain: "light"
    response: "all"
  - sentences:
      - "<which> <luminile> <is> {on_off_states:state} [<din>] {area}"
    example: "X X"
    inferred_domain: "light"
    response: "which"
  - sentences:
      - "<how_many> <luminile> <is> {on_off_states:state} [<din>] {area}"
    example: "X X"
    inferred_domain: "light"
    response: "how_many"
  - sentences:
      - "<are_any> <ventilatoarele> {on_off_states:state} [<din>] {area}"
    example: "X X"
    inferred_domain: "fan"
    response: "any"
  - sentences:
      - "<are_all> <ventilatoarele> {on_off_states:state} [<din>] {area}"
    example: "X X"
    inferred_domain: "fan"
    response: "all"
  - sentences:
      - "<which> <ventilatoarele> <is> {on_off_states:state} [<din>] {area}"
    example: "X X"
    inferred_domain: "fan"
    response: "which"
  - sentences:
      - "<how_many> <ventilatoarele> <is> {on_off_states:state} [<din>] {area}"
    example: "X X"
    inferred_domain: "fan"
    response: "how_many"
  - sentences:
      - "<are_any> <intrerupatoarele> {on_off_states:state} [<din>] {area}"
    example: "X X"
    inferred_domain: "switch"
    response: "any"
  - sentences:
      - "<are_all> <intrerupatoarele> {on_off_states:state} [<din>] {area}"
    example: "X X"
    inferred_domain: "switch"
    response: "all"
  - sentences:
      - "<which> <intrerupatoarele> <is> {on_off_states:state} [<din>] {area}"
    example: "X X"
    inferred_domain: "switch"
    response: "which"
  - sentences:
      - "<how_many> <intrerupatoarele> <is> {on_off_states:state} [<din>] {area}"
    example: "X X"
    inferred_domain: "switch"
    response: "how_many"
  - sentences:
      - "<are_any> <lockable> {lock_states:state} [<din>] {area}"
    example: "X X"
    inferred_domain: "lock"
    response: "any"
  - sentences:
      - "<are_all> <lockable> {lock_states:state} [<din>] {area}"
    example: "X X"
    inferred_domain: "lock"
    response: "all"
  - sentences:
      - "<which> <lockable> <is> {lock_states:state} [<din>] {area}"
    example: "X X"
    inferred_domain: "lock"
    response: "which"
  - sentences:
      - "<how_many> <lockable> <is> {lock_states:state} [<din>] {area}"
    example: "X X"
    inferred_domain: "lock"
    response: "how_many"
