---
# HassGetState - device_class_state

# slots: domain, device_class, state
language: "ro"
data:
  # covers
  - sentences:
      - "<are_any> {cover_classes:device_class} {cover_states:state}"
    example: "sunt uși deschise"
    inferred_domain: "cover"
    response: "any"
  - sentences:
      - "<are_all> {cover_classes:device_class} {cover_states:state}"
    example: "sunt toate ușile deschise"
    inferred_domain: "cover"
    response: "all"
  - sentences:
      - "<which> {cover_classes:device_class} sunt {cover_states:state}"
    example: "care uși sunt deschise"
    inferred_domain: "cover"
    response: "which"
  - sentences:
      - "<how_many> {cover_classes:device_class} sunt {cover_states:state}"
    example: "câte uși sunt deschise"
    inferred_domain: "cover"
    response: "how_many"
