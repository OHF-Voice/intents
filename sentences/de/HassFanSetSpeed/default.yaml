---
# HassFanSetSpeed - default
# slots: {percentage}
# context_area: true

language: "de"
data:
  - sentences:
      - "[<setzen> [die ]]Geschwindigkeit (<luefter>|<luefter_genitiv>)[ <hier>] (auf|zu) {fan_speed:percentage}[%| Prozent]"
      - "<stelle> [die ]Geschwindigkeit (<luefter>|<luefter_genitiv>)[ <hier>] auf {fan_speed:percentage}[%| Prozent] ein"
      - "[Geschwindigkeit ](<luefter>|<luefter_genitiv>)[ <hier>][ auf] {fan_speed:percentage}[%| Prozent][ <setzen_end_of_sentence>]"
    example: "Stelle die Geschwindigkeit der Ventilatoren auf 50%"
    response: "default"
