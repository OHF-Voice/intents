---
# HassFanSetSpeed - area_only
# slots: {area}, {percentage}

language: "de"
data:
  - sentences:
      - "[<setzen> [die ]]Geschwindigkeit (<luefter>|<luefter_genitiv>) <area> (auf|zu) {fan_speed:percentage}[%| Prozent]"
      - "<stelle> [die ]Geschwindigkeit (<luefter>|<luefter_genitiv>) <area> auf {fan_speed:percentage}[%| Prozent] ein"
      - "[Geschwindigkeit ](<luefter>|<luefter_genitiv>) <area>[ auf] {fan_speed:percentage}[%| Prozent][ <setzen_end_of_sentence>]"
    example: "Ändere die Geschwindigkeit der Lüfter im Wohnzimmer auf 50%"
    response: "default"
