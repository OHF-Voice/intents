---
# HassFanSetSpeed - floor_only
# slots: {floor}, {percentage}

language: "de"
data:
  - sentences:
      - "[<setzen> [die ]]Geschwindigkeit (<luefter>|<luefter_genitiv>) <floor> (auf|zu) {fan_speed:percentage}[%| Prozent]"
      - "<stelle> [die ]Geschwindigkeit (<luefter>|<luefter_genitiv>) <floor> auf {fan_speed:percentage}[%| Prozent] ein"
      - "[Geschwindigkeit ](<luefter>|<luefter_genitiv>) <floor>[ auf] {fan_speed:percentage}[%| Prozent][ <setzen_end_of_sentence>]"
    example: "Ändere die Geschwindigkeit der Lüfter im Erdgeschoss auf 50%"
    response: "default"
