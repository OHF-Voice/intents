---
# HassTurnOn - area_device_class_cover
# example: odpri zavese v kuhinji
# slots:
# - area
# - device_class

language: "sl"
data:
  - sentences:
      - "<odpri> {cover_classes:device_class} [v|na] {area}"
      - "<odpri> [v|na] {area} {cover_classes:device_class}"
    example: "odpri zavese v kuhinji"
    inferred_domain: "cover"
    response: "cover_device_class"
