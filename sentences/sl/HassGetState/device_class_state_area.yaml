---
# HassGetState - device_class_state_area
language: "sl"
data:
  - sentences:
      - "<are_any> {area} {cover_classes:device_class} {cover_states:state}"
      - "<are_any> {cover_classes:device_class} {cover_states:state} [v|na] {area}"
    example: "ali so okna odklenjena"
    inferred_domain: "cover"
    response: "any"
  - sentences:
      - "<are_all> {area} {cover_classes:device_class} {cover_states:state}"
      - "<are_all> {cover_classes:device_class} {cover_states:state} [v|na] {area}"
    example: "ali so okna odklenjena"
    inferred_domain: "cover"
    response: "all"
  - sentences:
      - "<which> {area} {cover_classes:device_class} <is> {cover_states:state}"
      - "<which> {cover_classes:device_class} <is> {cover_states:state} [v|na] {area}"
    example: "ali so okna odklenjena"
    inferred_domain: "cover"
    response: "which"
  - sentences:
      - "<how_many> {area} {cover_classes:device_class} <is> {cover_states:state}"
      - "<how_many> {cover_classes:device_class} <is> {cover_states:state} [v|na] {area}"
    example: "ali so okna odklenjena"
    inferred_domain: "cover"
    response: "how_many"
