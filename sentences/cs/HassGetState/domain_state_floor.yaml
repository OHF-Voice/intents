---
# HassGetState - domain_state_floor
# slots:
# - floor
# - domain
# - state

language: "cs"
data:
  # ---- light ----
  - sentences:
      - "<je> <nektere> <svetla> ({on_off_states:state};<floor>)"
    example: "v prvním patře"
    inferred_domain: "light"
    response: "any"
  - sentences:
      - "<je> všechn(a|y) <svetla> ({on_off_states:state};<floor>)"
    example: "v prvním patře"
    inferred_domain: "light"
    response: "all"
  - sentences:
      - "<ktere> <svetla> (<floor>;<je> {on_off_states:state})"
    example: "v prvním patře"
    inferred_domain: "light"
    response: "which"
  - sentences:
      - "kolik světel (<floor>;<je> {on_off_states:state})"
    example: "v prvním patře"
    inferred_domain: "light"
    response: "how_many"
  # ---- fan ----
  - sentences:
      - "<je> <nektere> (ventilátory|větráky) ({on_off_states:state};<floor>)"
    example: "v prvním patře"
    inferred_domain: "fan"
    response: "any"
  - sentences:
      - "<je> všechn(a|y) (ventilátory|větráky) ({on_off_states:state};<floor>)"
    example: "v prvním patře"
    inferred_domain: "fan"
    response: "all"
  - sentences:
      - "<ktere> (ventilátory|větráky) (<floor>;<je> {on_off_states:state})"
    example: "v prvním patře"
    inferred_domain: "fan"
    response: "which"
  - sentences:
      - "kolik ventilátorů (<floor>;<je> {on_off_states:state})"
    example: "v prvním patře"
    inferred_domain: "fan"
    response: "how_many"
  # ---- switch ----
  - sentences:
      - "<je> <nektere> vypínače ({on_off_states:state};<floor>)"
    example: "v prvním patře"
    inferred_domain: "switch"
    response: "any"
  - sentences:
      - "<je> všechn(a|y) vypínače ({on_off_states:state};<floor>)"
    example: "v prvním patře"
    inferred_domain: "switch"
    response: "all"
  - sentences:
      - "<ktere> vypínače (<floor>;<je> {on_off_states:state})"
    example: "v prvním patře"
    inferred_domain: "switch"
    response: "which"
  - sentences:
      - "kolik vypínačů (<floor>;<je> {on_off_states:state})"
    example: "v prvním patře"
    inferred_domain: "switch"
    response: "how_many"
  # ---- lock ----
  - sentences:
      - "<je> <nektere> <lockable> ({lock_states:state};<floor>)"
    example: "v prvním patře"
    inferred_domain: "lock"
    response: "any"
  - sentences:
      - "<je> všechn(a|y) <lockable> ({lock_states:state};<floor>)"
    example: "v prvním patře"
    inferred_domain: "lock"
    response: "all"
  - sentences:
      - "<ktere> <lockable> (<floor>;<je> {lock_states:state})"
    example: "v prvním patře"
    inferred_domain: "lock"
    response: "which"
  - sentences:
      - "kolik <lockable> (<floor>;<je> {lock_states:state})"
    example: "v prvním patře"
    inferred_domain: "lock"
    response: "how_many"
