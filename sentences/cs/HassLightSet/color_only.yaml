---
# HassLightSet - color_only
# example: nastav barvu světel na červenou
# slots:
# - {color}
# - {domain}
# context_area: true

language: "cs"
data:
  - sentences:
      - "(<nastavit>|<zmenit>) [barvu] <svetla> na {color}"
    example: "nastav barvu světel na červenou"
    inferred_domain: "light"
    response: "color"
