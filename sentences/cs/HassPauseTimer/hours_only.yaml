---
# HassPauseTimer - hours_only
# slots: {start_hours}

language: "cs"
data:
  - sentences:
      - "<timer_pause> <casovac> [na] <start_hours>"
    example: "pozastav časovač na 1 hodinu"
    response: "default"
