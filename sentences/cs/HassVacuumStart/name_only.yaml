---
# HassVacuumStart - name_only
# example: spusť vysavač
# slots: {name}

language: "cs"
data:
  - sentences:
      - "(nastart(uj|ovat)|<spustit>|<zapnout>) {name}"
    example: "spusť vysavač"
    name_domains:
      - "vacuum"
    response: "default"
