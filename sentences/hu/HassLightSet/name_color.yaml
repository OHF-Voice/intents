---
# HassLightSet - name_color
# example: állítsd a nappali lámpa színét pirosra
# slots:
# - {name}
# - {color}
# name domains: light

language: "hu"

data:
  - sentences:
      - "[<set>] <name> [<light>] szín(űre|e|re|ét) {color}"
      - "[<set>] <name> [<light>] {color}"
    example: "állítsd a mennyezeti lámpa színét pirosra"
    name_domains:
      - "light"
    response: "color_1"
