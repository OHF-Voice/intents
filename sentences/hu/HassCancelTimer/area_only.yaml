---
# HassCancelTimer - area_only

language: "hu"
data:
  - sentences:
      - "(<timer_cancel>;[a|az] <area> időzítő(t|jét))"
      - "[a|az] <area> időzítő(t|jének) törlése"
    example: "konyha időzítőjének törlése"
    response: "default"
